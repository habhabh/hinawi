import { createCategoryAction, createProjectAction, createSellerAction } from "@/features/admin/actions";
const Field = ({ label, name, required = false, wide = false }: { label: string; name: string; required?: boolean; wide?: boolean }) => <div className={`field ${wide ? "field-wide" : ""}`}><label htmlFor={name}>{label}</label><input id={name} name={name} required={required} /></div>;
export function SellerForm() { return <form className="card form-grid" action={createSellerAction}><Field label="الاسم" name="name" required /><Field label="الرابط المختصر (اختياري)" name="slug" /><Field label="المسمى الوظيفي" name="jobTitle" /><Field label="الفرع" name="branch" /><Field label="رقم الاتصال E.164" name="phoneE164" /><Field label="واتساب E.164" name="whatsappE164" /><div className="field field-wide"><label htmlFor="bio">نبذة</label><textarea id="bio" name="bio" /></div><label><input type="checkbox" name="isActive" defaultChecked /> نشط</label><button className="button button-primary">إضافة البائع</button></form>; }
export function CategoryForm() { return <form className="card form-grid" action={createCategoryAction}><Field label="اسم القسم" name="name" required /><Field label="الرابط المختصر (اختياري)" name="slug" /><div className="field field-wide"><label htmlFor="description">الوصف</label><textarea id="description" name="description" /></div><label><input type="checkbox" name="isActive" defaultChecked /> نشط</label><button className="button button-primary">إضافة القسم</button></form>; }
export function ProjectForm() { return <form className="card form-grid" action={createProjectAction}><Field label="عنوان المشروع" name="title" required /><Field label="الرابط المختصر (اختياري)" name="slug" /><div className="field field-wide"><label htmlFor="summary">الملخص</label><textarea id="summary" name="summary" /></div><button className="button button-primary field-wide">إنشاء كمسودة</button></form>; }
