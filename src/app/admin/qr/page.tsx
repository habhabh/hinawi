import { adminLists } from "@/db/queries/admin";
export default async function QrPage() { const rows = await adminLists.qr(); return <><h1>روابط QR</h1><p className="muted">الرموز ثابتة حتى عند تغيير رابط البائع، ويمكن إعادة إسنادها من قاعدة البيانات أو واجهة الإدارة الممتدة.</p><div className="table-wrap"><table><thead><tr><th>البائع</th><th>الحالة</th><th>المسحات</th><th>آخر مسح</th><th>الرابط</th></tr></thead><tbody>{rows.map(({ qr, sellerName }) => <tr key={qr.id}><td>{sellerName}</td><td>{qr.isActive ? "فعال" : "معطل"}</td><td>{qr.scanCount}</td><td>{qr.lastScannedAt?.toLocaleString("ar-SA") || "—"}</td><td><a href={`/q/${qr.token}`} target="_blank">نسخ/فتح</a></td></tr>)}</tbody></table></div></>; }
