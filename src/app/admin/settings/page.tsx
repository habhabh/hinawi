import { getSettings } from "@/db/queries/public";
import { requirePermission } from "@/lib/auth/session";
export default async function SettingsPage() { await requirePermission("settings:write"); const settings = await getSettings(); return <><h1>إعدادات الشركة</h1><section className="card form-grid"><div className="field"><label>اسم الشركة</label><input defaultValue={settings.companyName} readOnly /></div><div className="field"><label>الاسم المختصر</label><input defaultValue={settings.shortName} readOnly /></div><div className="field"><label>اللون الأساسي</label><input type="color" defaultValue={settings.primaryColor} readOnly /></div><div className="field"><label>لون الخلفية</label><input type="color" defaultValue={settings.backgroundColor} readOnly /></div><p className="field-wide muted">تُدار القيم في جدول site_settings. بيانات التخزين وSMTP أسرار خادمية ولا تظهر هنا.</p></section></>; }
