import { getDashboardStats } from "@/db/queries/admin";
export default async function AnalyticsPage() { const stats = await getDashboardStats(30); return <><h1>التحليلات</h1><p className="muted">آخر 30 يومًا بتوقيت العرض Asia/Riyadh. تُخزن الأوقات في UTC.</p><section className="stats"><article className="stat">المشاهدات<strong>{stats.views}</strong></article><article className="stat">QR<strong>{stats.qrScans}</strong></article><article className="stat">واتساب<strong>{stats.whatsappClicks}</strong></article><article className="stat">اتصال<strong>{stats.phoneClicks}</strong></article><article className="stat">المشاركات<strong>{stats.shares}</strong></article><article className="stat">معدل التفاعل مع التواصل<strong>{stats.engagementRate}%</strong></article></section></>; }
