# تعليمات المشروع

- التطبيق عربي وRTL، والواجهة العامة Mobile-first.
- المشروع كيان مركزي ولا يُنسخ عند إسناده لأكثر من بائع.
- الصفحات العامة النشطة قابلة للفهرسة، وcanonical المشروع دائمًا `/works/[slug]` بلا `advisor`.
- لا تستخدم Supabase أو اتصال قاعدة بيانات من المتصفح. PostgreSQL وDrizzle يعملان على الخادم فقط.
- وجهة النشر Docker وCoolify؛ لا تعتمد على Vercel.
- لا تكشف أسرار قاعدة البيانات أو S3 أو SMTP، ولا تستخدم `NEXT_PUBLIC_` لأي سر.
- كل تغيير schema يحتاج SQL migration قابلة للمراجعة. لا تستخدم `drizzle-kit push` في الإنتاج.
- شغّل lint وtypecheck والاختبارات والبناء قبل إنهاء أي تعديل.
